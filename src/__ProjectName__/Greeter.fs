namespace __ProjectName__

/// Placeholder module so the template compiles and tests pass out of the box.
/// Delete it and replace it with your library's public API.
module Greeter =

    /// Returns a greeting for the given name.
    let greet (name: string) = $"Hello, {name}!"
